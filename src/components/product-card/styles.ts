import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    card: {
        width: '46%',
        padding: 12,
        alignItems: 'center',
        borderRadius: 10,
        borderWidth: 1,
        borderColor: '#cecac6',
        marginTop: 10
    },

    imageContainer: {
        width: '100%',
        height: 180,
    },

    image: {
        width: '100%',
        height: '100%',
    },

    productName: {
        color: '#0c0f28',
        textAlign: 'center',
    },

    productPrice: {
        color: '#171C43',
        fontWeight: 'bold',
        marginTop: 4,
    },

    list: {
    paddingHorizontal: 30,
    paddingBottom: 30,
}
})